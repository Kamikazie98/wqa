# پیاده‌سازی چت بات‌های تخصصی

این سند شامل توضیحات کامل پیاده‌سازی چت بات‌های تخصصی برای 4 حوزه است.

## 📋 حوزه‌های تخصصی

1. **روانشناسی و سلامت روان** (`psychology`)
2. **مشاوره املاک** (`real_estate`)
3. **مکانیک خودرو** (`mechanics`)
4. **مشاوره استعداد یابی** (`talent_assessment`)

---

## 🔧 تغییرات در بک‌اند (app.py)

### 1. اضافه کردن Expert Domain به ChatRequest

```python
class ChatRequest(BaseModel):
    messages: List[Message]
    session_id: Optional[str] = None
    reset: bool = False
    web_search: bool = False
    expert_domain: Optional[str] = None  # psychology, real_estate, mechanics, talent_assessment
```

### 2. System Prompts تخصصی

System prompts برای هر حوزه در `EXPERT_SYSTEM_PROMPTS` تعریف شده‌اند که شامل:
- تخصص‌های هر حوزه
- قوانین و دستورالعمل‌ها
- استفاده از آخرین اطلاعات علمی
- استفاده از web search برای اطلاعات به‌روز

### 3. استفاده از Expert Domain در Chat Stream

در endpoint `/chat/stream`:
- اگر `expert_domain` ارسال شود، system prompt مربوطه اضافه می‌شود
- `web_search` به صورت خودکار فعال می‌شود
- System prompt در ابتدای messages قرار می‌گیرد

---

## 📱 تغییرات در Flutter

### 1. صفحه Experts

**فایل:** `lib/features/experts/experts_page.dart`

صفحه جدید با 4 کارت برای انتخاب حوزه تخصصی:
- هر کارت شامل عنوان، توضیحات و آیکون است
- با کلیک روی هر کارت، صفحه چت تخصصی باز می‌شود

### 2. صفحه چت تخصصی

**فایل:** `lib/features/experts/experts_page.dart` (کلاس `ExpertChatPage`)

- UI مشابه چت عادی اما با عنوان تخصصی
- استفاده از `expertDomain` در ارسال پیام
- Empty state مخصوص برای هر متخصص

### 3. تغییرات در ChatController

**فایل:** `lib/controllers/chat_controller.dart`

- متد `sendMessage` حالا `expertDomain` را می‌پذیرد
- متد جدید `sendExpertMessage` برای راحتی استفاده
- برای expert domains، web_search همیشه فعال است

### 4. تغییرات در ChatRequest Model

**فایل:** `lib/models/chat_models.dart`

- اضافه شدن فیلد `expertDomain` به `ChatRequest`
- ارسال `expert_domain` در JSON request

### 5. اضافه شدن به Navigation

**فایل:** `lib/features/home/home_shell.dart`

- اضافه شدن `ExpertsPage` به لیست صفحات
- اضافه شدن navigation destination جدید با آیکون `verified_user`

---

## 🎯 ویژگی‌های کلیدی

### 1. System Prompts تخصصی

هر حوزه دارای system prompt مخصوص است که:
- نقش متخصص را تعریف می‌کند
- تخصص‌ها را مشخص می‌کند
- قوانین و دستورالعمل‌ها را تعیین می‌کند
- استفاده از آخرین اطلاعات علمی را تأکید می‌کند

### 2. استفاده از Web Search

برای تمام expert domains:
- Web search به صورت خودکار فعال است
- آخرین اطلاعات علمی از اینترنت دریافت می‌شود
- منابع علمی ذکر می‌شوند

### 3. پاسخ‌های تخصصی

هر متخصص:
- بر اساس آخرین تحقیقات علمی پاسخ می‌دهد
- از اصطلاحات تخصصی استفاده می‌کند
- منابع را ذکر می‌کند
- در صورت نیاز به متخصص دیگر ارجاع می‌دهد

---

## 📝 System Prompts

### روانشناسی (`psychology`)

**تخصص‌ها:**
- تشخیص و درمان اختلالات روانی
- رواندرمانی (CBT، DBT، روانکاوی)
- روانشناسی سلامت و مدیریت استرس
- روابط بین فردی و خانواده درمانی
- روانشناسی رشد و کودک

**قوانین:**
- بر اساس DSM-5 و ICD-11
- هرگز تشخیص قطعی نده
- در موارد بحرانی به اورژانس ارجاع بده
- منابع علمی را ذکر کن

### املاک (`real_estate`)

**تخصص‌ها:**
- ارزش‌گذاری و قیمت‌گذاری
- قوانین خرید و فروش
- سرمایه‌گذاری در املاک
- رهن و اجاره
- مالیات و عوارض
- بازسازی و بهینه‌سازی

**قوانین:**
- بر اساس آخرین قوانین ایران
- اطلاعات قیمتی با ذکر منطقه و تاریخ
- برای قراردادها به وکیل ارجاع بده
- ریسک‌ها را شفاف بیان کن

### مکانیک (`mechanics`)

**تخصص‌ها:**
- تشخیص و تعمیر موتور
- سیستم‌های برقی
- ترمز و فرمان
- تعلیق و کمک‌فنرها
- گیربکس
- تهویه و کولر
- نگهداری و سرویس

**قوانین:**
- بر اساس آخرین تکنولوژی‌ها
- علت، تشخیص و راه حل را توضیح بده
- در مورد ایمنی هشدار بده
- کدهای خطا (OBD) را تفسیر کن
- هزینه‌های تقریبی را ذکر کن

### استعداد یابی (`talent_assessment`)

**تخصص‌ها:**
- ارزیابی استعدادها
- تست‌های شخصیت و شغلی
- راهنمایی انتخاب رشته و شغل
- برنامه‌ریزی مسیر شغلی
- توسعه مهارت‌ها
- انگیزش و هدف‌گذاری

**قوانین:**
- بر اساس آخرین نظریه‌های روانشناسی شغلی
- از تست‌های معتبر استفاده کن
- هر فرد را منحصر به فرد در نظر بگیر
- برای ارزیابی دقیق‌تر به مراکز معتبر ارجاع بده

---

## 🚀 نحوه استفاده

### در Flutter:

1. کاربر به صفحه "متخصصان" می‌رود
2. یکی از 4 حوزه را انتخاب می‌کند
3. صفحه چت تخصصی باز می‌شود
4. با متخصص تخصصی گفتگو می‌کند
5. متخصص بر اساس آخرین اطلاعات علمی پاسخ می‌دهد

### در بک‌اند:

1. Request با `expert_domain` ارسال می‌شود
2. System prompt مربوطه اضافه می‌شود
3. Web search فعال می‌شود
4. پاسخ تخصصی بر اساس system prompt و آخرین اطلاعات تولید می‌شود

---

## ✅ چک‌لیست پیاده‌سازی

### بک‌اند ✅
- [x] اضافه کردن `expert_domain` به `ChatRequest`
- [x] تعریف `EXPERT_SYSTEM_PROMPTS` برای 4 حوزه
- [x] اضافه کردن `_get_expert_system_prompt` function
- [x] استفاده از expert domain در chat stream
- [x] فعال کردن web_search برای expert domains

### Flutter ✅
- [x] ایجاد `ExpertsPage` با 4 کارت
- [x] ایجاد `ExpertChatPage` برای چت تخصصی
- [x] اضافه کردن `expertDomain` به `ChatRequest`
- [x] اضافه کردن `sendExpertMessage` به `ChatController`
- [x] اضافه شدن به navigation bar

---

## 🧪 تست کردن

### 1. تست روانشناسی

```
سوال: "علائم افسردگی چیست؟"
انتظار: پاسخ تخصصی بر اساس DSM-5 با ذکر منابع
```

### 2. تست املاک

```
سوال: "قیمت آپارتمان 100 متری در تهران چقدر است؟"
انتظار: اطلاعات قیمتی با ذکر منطقه و تاریخ، ارجاع به web search
```

### 3. تست مکانیک

```
سوال: "ماشینم روشن نمی‌شود، چه مشکلی می‌تواند باشد؟"
انتظار: لیست علل احتمالی، روش تشخیص، راه حل‌ها
```

### 4. تست استعداد یابی

```
سوال: "چگونه استعدادهایم را پیدا کنم؟"
انتظار: راهنمایی بر اساس تست‌های معتبر، پیشنهاد مراکز مشاوره
```

---

## 📚 نکات مهم

1. **اطلاعات علمی:** تمام متخصصان از آخرین اطلاعات علمی استفاده می‌کنند
2. **Web Search:** برای expert domains همیشه فعال است
3. **ارجاع:** در صورت نیاز، به متخصص یا مرکز مناسب ارجاع می‌دهند
4. **ایمنی:** در موارد بحرانی (مثل خودکشی) به اورژانس ارجاع می‌دهند
5. **قوانین:** در مورد قوانین (مثل قراردادها) به وکیل ارجاع می‌دهند

---

## 🎉 نتیجه

با این پیاده‌سازی:
- ✅ کاربران می‌توانند با متخصصان تخصصی گفتگو کنند
- ✅ پاسخ‌ها بر اساس آخرین اطلاعات علمی هستند
- ✅ هر حوزه دارای system prompt مخصوص است
- ✅ Web search برای اطلاعات به‌روز فعال است
- ✅ UI/UX مناسب برای انتخاب و استفاده از متخصصان

